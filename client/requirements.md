## Packages
framer-motion | For smooth minimal animations and page transitions
lucide-react | For clean, modern icons

## Notes
- Endpoints are fetched from GET /api/endpoints
- Scrape action triggered via POST /api/scrape
- External links are constructed by prepending 'https://api.bk9.dev' to the endpoint 'link' property
